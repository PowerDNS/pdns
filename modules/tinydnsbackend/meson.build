sources = [
  # TODO Change this into a libpdns library
  '../../pdns/cdb.cc',
  'tinydnsbackend.cc',
]

if tinydns_module
  static_library('tinydnsbackend', sources)
endif

if tinydns_dyn_module
endif
