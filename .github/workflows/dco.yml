name: Test Developer Certificate of Origin

on:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: KineticCafe/actions-dco@5f160e3ee086af8fc0fb567b8add0937d074123e # 3.2.0
        with:
          exempt-authors: |
            @powerdns.com
            @open-xchange.com
