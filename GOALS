be able to specify:
	- which modules to build dynamically
	- which modules should be in the main binary
	
	- configuration options for the different modules

allow building of:
	- static binary with modules built in
		static rpm, dep
	- separate rpms/debs per module
	- separate rpm/deb for the binary
	- one big module

