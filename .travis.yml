os:
  - linux
  - osx
language: cpp
env:
  - TEST_SUITE=regression_tests
  - TEST_SUITE=regression_tests_nobackend
  - TEST_SUITE=regression_tests_recursor
  - TEST_SUITE=regression_tests_api
  - TEST_SUITE=ALL
compiler:
  - gcc
  - clang
before_script: travis/prepare.sh
script:
  - travis/build.sh
  - "travis/test.sh $TEST_SUITE"
#notifications:
  #  irc:
  #  channels:
  #    - "irc.oftc.net#powerdns-dev"
  #  template:
  #    - "%{author} @ %{repository} / %{branch} - Build:#%{build_number} : %{message} - Changes: %{compare_url} - Build details: %{build_url}"
  #  use_notice: true
  #  skip_join: true
