Features
========

The following documents describe several (DNS) features or the PowerDNS Authoritative Server.

.. toctree::
    :maxdepth: 2
    :glob:

    ../modes-of-operation
    ../performance
    ../dnssec/index
    ../domainmetadata
    ../dnsupdate
    ../catalog
    ../tsig
    ../views
    ../lua-records/index
